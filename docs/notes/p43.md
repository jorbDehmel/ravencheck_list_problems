
```lisp
; List monad laws
(declare-datatype
  list (par (a) ((nil) (cons (head a) (tail (list a))))))
(define-fun-rec
  ++
  (par (a) (((x (list a)) (y (list a))) (list a)))
  (match x
    ((nil y)
     ((cons z xs) (cons z (++ xs y))))))
(define-fun-rec
  >>=
  (par (a b) (((x (list a)) (y (=> a (list b)))) (list b)))
  (match x
    ((nil (_ nil b))
     ((cons z xs) (++ (@ y z) (>>= xs y))))))
(prove
  (par (a b)
    (forall ((x a) (f (=> a (list b))))
      (= (>>= (cons x (_ nil a)) f) (@ f x)))))
```

```rust
#[ravencheck::check_module]
#[allow(dead_code)]
mod p43 {
  #[define]
  #[derive(PartialEq, Clone)]
  pub enum LinkedList<T> {
    Nil,
    Cons(T, Box<LinkedList<T>>),
  }

  #[define]
  #[recursive]
  pub fn append<A>(x: LinkedList<A>, y: LinkedList<A>) -> LinkedList<A> {
    match x {
      LinkedList::<A>::Nil => y,
      LinkedList::<A>::Cons(z, xs) => LinkedList::<A>::Cons(z, Box::new(append::<A>(*xs, y)))
    }
  }

  #[define]
  #[recursive]
  #[total]
  fn map_concat<A, B: PartialEq>(x: LinkedList<A>,
      y: fn(A) -> LinkedList<B>) -> LinkedList<B> {
    match x {
      LinkedList::<A>::Nil => LinkedList::<B>::Nil,
      LinkedList::<A>::Cons(z, xs) => append::<B>(
        y(z),
        map_concat::<A, B>(*xs, y)
      )
    }
  }

  #[declare]
  #[phantom]
  fn f<A, B>(_: A) -> LinkedList<B> {}

  #[annotate]
  #[for_type(LinkedList<A> => <A>)]
  #[for_type(LinkedList<B> => <B>)]
  fn p43<A, B>(x: A) -> bool {
    map_concat::<A, B>(
      LinkedList::<A>::Cons(
        x,
        LinkedList::<A>::Nil
      ),
      f::<A, B>
    ) == f::<A, B>(x)
  }
}

```

This fails in case `Cons`, somehow. We will begin by expanding.

```rust
map_concat(
  Cons(
    x,
    Nil
  ),
  f
) == f(x)

// Definition of map_concat

append(
  f(x),
  map_concat(Nil, f)
) == f(x)

// Does it know that map_concat(Nil, f) == Nil?
```

We will now add a lemma based on this final question.

```rust
#[ravencheck::check_module]
#[allow(dead_code)]
mod p43 {
  #[define]
  #[derive(PartialEq, Clone)]
  pub enum LinkedList<T> {
    Nil,
    Cons(T, Box<LinkedList<T>>),
  }

  #[define]
  #[recursive]
  pub fn append<A>(x: LinkedList<A>, y: LinkedList<A>) -> LinkedList<A> {
    match x {
      LinkedList::<A>::Nil => y,
      LinkedList::<A>::Cons(z, xs) => LinkedList::<A>::Cons(z, Box::new(append::<A>(*xs, y)))
    }
  }

  #[define]
  #[recursive]
  #[total]
  fn map_concat<A, B: PartialEq>(x: LinkedList<A>,
      y: fn(A) -> LinkedList<B>) -> LinkedList<B> {
    match x {
      LinkedList::<A>::Nil => LinkedList::<B>::Nil,
      LinkedList::<A>::Cons(z, xs) => append::<B>(
        y(z),
        map_concat::<A, B>(*xs, y)
      )
    }
  }


  #[declare]
  #[phantom]
  fn f<A, B>(_: A) -> LinkedList<B> {}

  #[annotate]
  #[for_type(LinkedList<A> => <A>)]
  #[for_type(LinkedList<B> => <B>)]
  fn lemma_1<A, B>() -> bool {
    map_concat::<A, B>(LinkedList::<A>::Nil, f::<A, B>)
    ==
    LinkedList::<B>::Nil
  }

  #[annotate]
  #[for_type(LinkedList<A> => <A>)]
  #[for_type(LinkedList<B> => <B>)]
  fn p43<A, B>(x: A) -> bool {
    map_concat::<A, B>(
      LinkedList::<A>::Cons(
        x,
        LinkedList::<A>::Nil
      ),
      f::<A, B>
    ) == f::<A, B>(x)
  }
}

```

And now it verifies!
